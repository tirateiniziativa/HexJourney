{
  "name": "@hexjourney/worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p tsconfig.json",
    "dry-run": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "@hexjourney/shared": "*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "~5.6.0",
    "wrangler": "^4"
  }
}
